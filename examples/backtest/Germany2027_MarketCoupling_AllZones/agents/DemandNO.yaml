Agents:
  # Norway's own real annual demand total (126,205,458 MWh, Eurostat 2023),
  # shaped using Germany's own real 2027 demand pattern (same borrowed-shape precedent as
  # every other zone in this build).
  - Type: DemandTrader
    Id: 7500
    Attributes:
      Loads:
        - ValueOfLostLoad: 3000.0
          DemandSeries: "./timeseries/load_no_2027_shapeFromDE.csv"
