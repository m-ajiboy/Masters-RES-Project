Agents:
  # Switzerland's own real annual demand total (61,070,971 MWh, Eurostat 2023),
  # shaped using Germany's own real 2027 demand pattern (same borrowed-shape precedent as
  # every other zone in this build).
  - Type: DemandTrader
    Id: 12500
    Attributes:
      Loads:
        - ValueOfLostLoad: 3000.0
          DemandSeries: "./timeseries/load_ch_2027_shapeFromDE.csv"
