Agents:
  # Czech Republic's own real annual demand total (56,850,161 MWh, Eurostat 2023),
  # shaped using Germany's own real 2027 demand pattern (same borrowed-shape precedent as
  # every other zone in this build).
  - Type: DemandTrader
    Id: 5500
    Attributes:
      Loads:
        - ValueOfLostLoad: 3000.0
          DemandSeries: "./timeseries/load_cz_2027_shapeFromDE.csv"
