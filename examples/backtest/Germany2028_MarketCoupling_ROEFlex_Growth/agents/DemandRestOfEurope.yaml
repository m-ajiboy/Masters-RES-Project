Agents:
  # Rest-of-Europe aggregate demand - real ERAA-growth-rate-projected 2028 total
  # (1,223,126 MWh), applied to the real 2024 Eurostat baseline (see
  # build_roeflex_growth_timeseries.py). Shape still borrowed from Germany's own real 2028
  # demand pattern.
  - Type: DemandTrader
    Id: 9500
    Attributes:
      Loads:
        - ValueOfLostLoad: 3000.0
          DemandSeries: "./timeseries/load_restofeurope_2028_shapeFromDE.csv"
